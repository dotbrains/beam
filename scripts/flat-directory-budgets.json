{
  "default_files": 12,
  "directories": {
    ".": {
      "limit": 14,
      "reason": "Small Go CLI root with standard project metadata."
    },
    "cmd": {
      "limit": 20,
      "reason": "Cobra command files stay flat until command families need subpackages."
    },
    "internal/beam": {
      "limit": 22,
      "reason": "Core domain package keeps HTTP, store, observability, callback delivery, provider diagnostics, validation, and focused regression tests together until a durable subpackage boundary is justified."
    }
  }
}
